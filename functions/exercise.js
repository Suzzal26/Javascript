//to reverse a number
/*
1.Write a function called reverser
2.Call the reverser with parameter
3.Invoke that Function with data
4.Typecast that Number to String
5.Get the length of the String
6.Loop in opposite way
7.Return the value */
/* const reverse=(num)=>{
    const data=String(num);
    const length0fData=data.length;
    for(let i=length0fData; i>=0;i--){
        console.log(data[i]);
    }
} */
/* function reverseNumber(num){
let reverseNum=0;
while(num!==0){
    const digit=number%10;
    reversedNum=reverseNum*10+digit;
    num=(num-digit)/10;
}
return reverseNum;
}
const original=Number(prompt("State the number"));
const reversed=reverseNumber(num1)
console.log(`Reversed Number ${original}`) */

// JavaScript function that accepts a string as a parameter and counts the number of vowels
//  within the string. Note : As the letter 'y' can be regarded as both a vowel and a consonant,
//   we do not count 'y' as vowel here. Example string : 'The quick brown fox'

function countVowels(str){
    const lowerstr= 
}